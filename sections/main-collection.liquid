<section class="section-padding"><div class="page-width"><h1>{{ collection.title }}</h1>{{ collection.description }}<div class="product-grid product-grid--4">{% for product in collection.products %}{% render 'product-card', product: product %}{% else %}<p>No products found.</p>{% endfor %}</div></div></section>{% schema %}{"name":"Main collection","settings":[]}{% endschema %}
